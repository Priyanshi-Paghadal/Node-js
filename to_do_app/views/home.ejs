<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        .flex {
            display: flex;
        }

        .justify-between {
            justify-content: space-between;
        }

        .inner {
            width: 500px;
            background-color: #fff;
            min-height: 500px;
            border-radius: 10px;
            box-shadow: rgba(50, 50, 93, 0.25) 0px 2px 5px -1px, rgba(0, 0, 0, 0.3) 0px 1px 3px -1px;
        }

        .main {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        h1 {
            text-align: center;
            background-color: #4982a5;
            color: #fff;
            padding: 10px 0;
            border-radius: 10px;
        }

        ul {
            list-style: none;
        }

        input[type='submit'] {
            background-color: green;
            color: #fff;
            border: none;
            padding: 8px 25px;
            border-radius: 5px;
        }

        form {
            padding: 30px;
            justify-content: space-between;
        }

        label,
        p {
            font-size: 17px;
        }

        input[type='text'] {
            padding: 5px 10px;
            width: 250px;
        }

        input[type='checkbox'] {
            width: 17px;
            margin-right: 30px;
            accent-color: green;
        }
        p{
            border-left: 2px double #f6dbd8;
            height: 50px;
            padding-left: 20px;
            line-height: 50px;
        }
        a{
            display: inline-block;
            text-decoration: none;
        }

        ul {
            padding: 0 40px;
            border-block: 1px solid #f2f0f1;
            align-items: center;
        }
        .edit{
            background-color: #4982a5;
            color: #fff;
            padding: 5px 10px;
            border-radius: 5px;
        }
        .remove{
            background-color: red;
            color: #fff;
            padding: 5px 10px;
            border-radius: 5px;
            margin-left: 10px;
        }
    </style>
</head>

<body>

    <div class="main">
        <div class="inner">
            <h1>To-Do App </h1>
            <form action="/addData" method="post" class="flex">
                <div>
                    <label>Add Task : </label>
                    <input type="text" name="name">
                </div>
                <input type="submit" value="ADD">
            </form>

            <% allTask.map((item)=>{ %>
                <ul class="flex justify-between">
                    <li class="flex">
                        <input type="checkbox" name="check">
                        <p>
                            <%= item.name %>
                        </p>
                    </li>
                    <li>
                        <a href="/edit/<%= item._id %>" class="edit">Edit</a>
                        <a href="/delete?id=<%= item._id %>" class="remove">Remove</a>
                    </li>
                </ul>
                <% }) %>
        </div>
    </div>

</body>

</html>